html lang="en"
  head
    title
      | Customer Accounts Concierge Error Page

    meta name="viewport" content="width=device-width"

    scss:
      html,
      body {
        font-family: sans-serif;
        -ms-text-size-adjust: 100%;
        -webkit-text-size-adjust: 100%;
        height: 100%;
        -webkit-font-smoothing: antialiased;
        text-align: center;
        font-size: 16px;
        color: #333333;
      }

      p {
        -webkit-margin-after: 0px;
        -webkit-margin-before: 0px;
        line-height: 1.4;
        margin-bottom: 16px;
      }

      .frcp-error {
        background: white;
        max-width: 800px;
        margin: auto;
        padding: 48px 24px;
        height: 100%;
        border-radius: 40px;

        &__title {
          font-size: 40px;
          line-height: 60px;
          margin: 32px 0;
        }

        &__quote {
          font-size: 20px;
          margin: 32px 0;
          padding: 20px;
          border: 1px solid #E5E5E5;
          background: #f3f3f3;
        }

        &__back-button {
          display: inline-block;
          padding: 8px 64px;
          border: 2px solid #167df0;
          color: #167df0;
          text-decoration: none;
          border-radius: 20px;

          &:hover {
            scale: 1.1;
          }
        }

        &__logo-wrapper {
          display: flex;
          justify-content: space-between;
          align-items: center;
        }

        &__logo {
          max-width: 200px;
          height: 100%;
        }
      }

  body 
    main class='frcp-error'
      = yield
